# Intent (MVP.md §3)

Add a scheme-finder feature under /features/schemes that calls the live gov schemes API, validates the user's bio input, and explains eligibility. Add tests. Do not touch anything outside /features/schemes.
