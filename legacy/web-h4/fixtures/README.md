# Shared local fixture — the single source for both clients

Written from MVP.md §6 so the UI works with Core down (`/runs/demo?replay=local`).
`vr/Assets/SpatialSOC/Fixtures/` holds an unchanged copy; edit here, then copy. Never edit the VR copy.
Stand-in only: Core's `core/fixtures/runs/drifting/` is the real recorded run.
